diff --git a/dist/index.cjs b/dist/index.cjs
index eec5f8b4776a15d023c6fe6c0a476ff07a9eb14c..ffd477a8461545999372ac41878d703a8566197e 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -1,6 +1,7 @@
 'use strict';
 
 var xmldom = require('@xmldom/xmldom');
+var colorString = require('color-string').default;
 
 // During an iterative conversion (see tree-to-latex) every child is converted
 // before its parent and its LaTeX is stored in this memo. While a memo is
@@ -149,8 +150,8 @@ const allMathOperatorsByChar = {
     '&#x2037;': '```',
     '&#x2036;': '``',
     '&#x2035;': '`',
-    '&#x2034;': "'''",
-    '&#x2033;': "''",
+    '&#x2034;': '\\prime\\prime\\prime',
+    '&#x2033;': '\\prime\\prime',
     '&#x201F;': '``',
     '&#x201E;': ',,',
     '&#x201B;': '`',
@@ -198,12 +199,12 @@ const allMathOperatorsByChar = {
     '&#x266F;': '\\#',
     '&#x266E;': '',
     '&#x266D;': '',
-    '&#x2032;': "'",
+    '&#x2032;': '\\prime',
     '&lt;>': '<>',
     '**': '\\star\\star',
     '&#x2207;': '\\nabla',
     '&#x2202;': '\\partial',
-    '&#x2299;': '\\bigodot',
+    '&#x2299;': '\\odot',
     '&#xAC;': '\\neg',
     '&#x2222;': '\\measuredangle',
     '&#x2221;': '\\measuredangle',
@@ -605,8 +606,8 @@ const allMathOperatorsByChar = {
     '&#x2ACF;': '',
     '&#x2ACE;': '',
     '&#x2ACD;': '',
-    '&#x2ACC;': '\\underset{\\neq}{\\supset}',
-    '&#x2ACB;': '\\underset{\\neq}{\\subset}',
+    '&#x2ACC;': '\\supsetneqq',
+    '&#x2ACB;': '\\subsetneqq',
     '&#x2ACA;': '\\underset{\\approx}{\\supset}',
     '&#x2AC9;': '\\underset{\\approx}{\\subset}',
     '&#x2AC8;': '\\underset{\\sim}{\\supset}',
@@ -682,9 +683,9 @@ const allMathOperatorsByChar = {
     '&#x2A80;': '',
     '&#x2A7F;': '',
     '&#x2A7E;&#x338;': '\\bcancel{\\geq}',
-    '&#x2A7E;': '\\geq',
+    '&#x2A7E;': '\\geqslant',
     '&#x2A7D;&#x338;': '\\bcancel{\\leq}',
-    '&#x2A7D;': '\\leq',
+    '&#x2A7D;': '\\leqslant',
     '&#x2A7C;': '',
     '&#x2A7B;': '',
     '&#x2A7A;': '',
@@ -1039,7 +1040,7 @@ const allMathOperatorsByChar = {
     '&#x25B5;': '\\triangle',
     '&#x25B4;': '\\triangle',
     '&#x25B3;': '\\triangle',
-    '&#x25B1;': '\\square',
+    '&#x25B1;': '\\text{▱}',
     '&#x25B0;': '\\square',
     '&#x25AF;': '\\square',
     '&#x25AE;': '\\square',
@@ -1094,7 +1095,7 @@ const allMathOperatorsByChar = {
     '&#x2244;': '\\not\\simeq',
     '&#x2243;': '\\simeq',
     '&#x2241;': '\\not\\sim',
-    '&#x2226;': '\\not\\parallel',
+    '&#x2226;': '\\nparallel',
     '&#x2225;': '\\parallel',
     '&#x2224;': '\\not|',
     '&#x221D;': '\\propto',
@@ -1134,7 +1135,7 @@ const allMathOperatorsByChar = {
     '&#x220C;': '\\not\\in',
     '&#x2209;': '\\notin',
     '&#x2208;': '\\in',
-    '&#x2201;': 'C',
+    '&#x2201;': '\\complement',
     '&#x2204;': '\\nexists',
     '&#x2203;': '\\exists',
     '&#x2200;': '\\forall',
@@ -1276,8 +1277,8 @@ const allMathOperatorsByGlyph = {
     '‷': '```',
     '‶': '``',
     '‵': '`',
-    '‴': "'''",
-    '″': "''",
+    '‴': '\\prime\\prime\\prime',
+    '″': '\\prime\\prime',
     '‟': '``',
     '„': ',,',
     '‛': '`',
@@ -1324,12 +1325,12 @@ const allMathOperatorsByGlyph = {
     '♯': '\\#',
     '♮': '',
     '♭': '',
-    '′': "'",
+    '′': '\\prime',
     '<>': '<>',
     '**': '\\star\\star',
     '∇': '\\nabla',
     '∂': '\\partial',
-    '⊙': '\\bigodot',
+    '⊙': '\\odot',
     '¬': '\\neg',
     '∢': '\\measuredangle',
     '∡': '\\measuredangle',
@@ -1730,8 +1731,8 @@ const allMathOperatorsByGlyph = {
     '⫏': '',
     '⫎': '',
     '⫍': '',
-    '⫌': '\\underset{\\neq}{\\supset}',
-    '⫋': '\\underset{\\neq}{\\subset}',
+    '⫌': '\\supsetneqq',
+    '⫋': '\\subsetneqq',
     '⫊': '\\underset{\\approx}{\\supset}',
     '⫉': '\\underset{\\approx}{\\subset}',
     '⫈': '\\underset{\\sim}{\\supset}',
@@ -1807,9 +1808,9 @@ const allMathOperatorsByGlyph = {
     '⪀': '',
     '⩿': '',
     '⩾̸': '\\bcancel{\\geq}',
-    '⩾': '\\geq',
+    '⩾': '\\geqslant',
     '⩽̸': '\\bcancel{\\leq}',
-    '⩽': '\\leq',
+    '⩽': '\\leqslant',
     '⩼': '',
     '⩻': '',
     '⩺': '',
@@ -2167,7 +2168,7 @@ const allMathOperatorsByGlyph = {
     '▵': '\\triangle',
     '▴': '\\triangle',
     '△': '\\triangle',
-    '▱': '\\square',
+    '▱': '\\text{▱}',
     '▰': '\\blacksquare',
     '▯': '\\square',
     '▮': '\\blacksquare',
@@ -2222,7 +2223,7 @@ const allMathOperatorsByGlyph = {
     '≄': '\\not\\simeq',
     '≃': '\\simeq',
     '≁': '\\not\\sim',
-    '∦': '\\not\\parallel',
+    '∦': '\\nparallel',
     '∥': '\\parallel',
     '∤': '\\not|',
     '∝': '\\propto',
@@ -2262,7 +2263,7 @@ const allMathOperatorsByGlyph = {
     '∌': '\\not\\in',
     '∉': '\\notin',
     '∈': '\\in',
-    '∁': 'C',
+    '∁': '\\complement',
     '∄': '\\nexists',
     '∃': '\\exists',
     '∀': '\\forall',
@@ -2469,10 +2470,17 @@ const allMathOperatorsByGlyph = {
  * `<mi>`/`<mo>` converters to translate symbol content into LaTeX.
  */
 const allMathSymbolsByChar = {
+    '&#x22A5;': '\\perp',
+    '&#x2ACB;': '\\subsetneqq',
+    '&#x2ACC;': '\\supsetneqq',
+    '&#x2226;': '\\nparallel',
+    '&#x2201;': '\\complement',
+    '&#x2204;': '\\nexists',
+    '&#x25B1;': '\\text{▱}',
     '&#xA0;': '\\textrm{ }',
     '&#x2203;': '\\exists',
     '&#x2200;': '\\forall',
-    '&#x21D4;': '\\iff',
+    '&#x21D4;': '\\Leftrightarrow',
     '&#x21D2;': '=>',
     '&#xAC;': '\\neg',
     '&#x2124;': '\\mathbb{Z}',
@@ -2506,7 +2514,7 @@ const allMathSymbolsByChar = {
     '&#x2227;': '\\wedge',
     '&#x220F;': '\\prod',
     '&#x2211;': '\\sum',
-    '&#x2299;': '\\bigodot',
+    '&#x2299;': '\\odot',
     '&#x2297;': '\\bigoplus',
     '&#x2295;': 'o+',
     '&#x2218;': '@',
@@ -2566,6 +2574,12 @@ const allMathSymbolsByChar = {
  * converters to translate symbol content into LaTeX.
  */
 const allMathSymbolsByGlyph = {
+    '⊥': '\\perp',
+    '⫋': '\\subsetneqq',
+    '⫌': '\\supsetneqq',
+    '∦': '\\nparallel',
+    '∁': '\\complement',
+    '∄': '\\nexists',
     ' ': '\\textrm{ }',
     // Invisible operators (U+2061-U+2064): no visual rendering in LaTeX.
     '⁡': '',
@@ -2574,7 +2588,7 @@ const allMathSymbolsByGlyph = {
     '⁤': '',
     '∃': '\\exists',
     '∀': '\\forall',
-    '⇔': '\\iff',
+    '⇔': '\\Leftrightarrow',
     '⇒': '\\Rightarrow',
     '¬': '\\neg',
     '□': '\\square',
@@ -2603,7 +2617,7 @@ const allMathSymbolsByGlyph = {
     '∧': '\\wedge',
     '∏': '\\prod',
     '∑': '\\sum',
-    '⊙': '\\bigodot',
+    '⊙': '\\odot',
     '⊗': '\\bigoplus',
     '⊕': 'o+',
     '∘': '@',
@@ -2868,7 +2882,7 @@ const allMathSymbolsByGlyph = {
     '→': '\\rightarrow',
     '‡': '\\ddagger',
     '・': '\\cdot',
-    '▱': '\\square',
+    '▱': '\\text{▱}',
     '∆': '\\Delta',
     ἱ: 'i',
     '∡': '\\angle',
@@ -3586,6 +3600,9 @@ class MI {
         if (normalizedValue === ' ')
             return Character.apply(normalizedValue);
         const trimmedValue = normalizedValue.trim();
+        if (/^(sin|cos|tan|cot|sec|csc|arcsin|arccos|arctan|log|lg|ln|exp|lim|min|max|sup|inf|det|gcd)$/.test(trimmedValue)) {
+            return this.wrapInMathVariant('\\' + trimmedValue, this.getMathVariant(this._mathmlElement.attributes));
+        }
         const convertedChar = Character.apply(trimmedValue);
         const parsedChar = this.utf8Converter.convert(convertedChar);
         if (parsedChar !== convertedChar)
@@ -3606,7 +3623,7 @@ class MI {
             case 'italic':
                 return `\\mathit{${value}}`;
             case 'bold-italic':
-                return `\\mathbf{\\mathit{${value}}}`;
+                return `\\mathbfit{${value}}`;
             case 'double-struck':
                 return `\\mathbb{${value}}`;
             case 'bold-fraktur':
@@ -4427,6 +4444,8 @@ class MText {
      */
     convert() {
         const { attributes, value } = this._mathmlElement;
+        // Keep literal textbook glyphs and their combining marks in a single text run.
+        if (/^[\s⫋⫌⫽⃥▱∁]+$/u.test(value)) return new TextCommand(attributes.mathvariant).apply(value);
         return [...value]
             .map((char) => {
             // if is a letter, number or space, return it
@@ -4454,9 +4473,13 @@ class MText {
         }, [])
             .map((char) => {
             if (!char.isAlphanumeric) {
+                // DSH geometry glyphs remain text atoms when their weight changes.
+                if (attributes.mathvariant && ['⫽', '▱'].includes(char.value)) {
+                    return new TextCommand(attributes.mathvariant).apply(char.value);
+                }
                 return new MI({
                     name: 'mi',
-                    attributes: {},
+                    attributes,
                     children: [],
                     value: char.value,
                 }).convert();
@@ -4679,6 +4702,13 @@ class GenericUnderOver {
         if (childrenLength !== 2)
             throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
         const content = mathMLElementToLaTeXConverter(children[0]).convert();
+        if (name === 'mover' && this._mathmlElement.attributes.accent === 'true') {
+            const mark = children[1];
+            const stretch = mark.attributes.stretchy === 'true';
+            const command = { '⃗': 'vec', '→': 'overrightarrow', '^': stretch ? 'widehat' : 'hat',
+                'ˉ': 'bar', '‾': 'overline', '~': stretch ? 'widetilde' : 'tilde', '˙': 'dot', '¨': 'ddot' }[mark.value];
+            if (command) return `\\${command}{${content}}`;
+        }
         const accent = mathMLElementToLaTeXConverter(children[1]).convert();
         return this._applyCommand(content, accent);
     }
@@ -5106,6 +5136,25 @@ class TableAnnotator {
  * @param root - the root element of the tree to convert.
  * @returns the LaTeX string for the whole tree.
  */
+
+// DSH Teacher: retain colors and mathematical bold italics when reopening saved math.
+const withMathColors = (node, latex) => {
+    if (node.attributes?.fontweight === 'bold') latex = `\\boldsymbol{${latex}}`;
+    if (['mn', 'mo'].includes(node.name) || (node.name === 'mi' && node.children.length > 0)) {
+        const command = { bold: 'mathbf', italic: 'mathit', 'bold-italic': 'mathbfit' }[node.attributes?.mathvariant];
+        if (command) latex = `\\${command}{${latex}}`;
+    }
+    for (const [attribute, command] of [['mathcolor', 'textcolor'], ['mathbackground', 'colorbox']]) {
+        const value = node.attributes?.[attribute] || (attribute === 'mathcolor' ? node.attributes?.color : undefined);
+        if (!value) continue;
+        const rgb = colorString.get.rgb(value);
+        if (!rgb || rgb[3] !== 1) throw new Error('Equation colors must be opaque RGB colors');
+        const color = '#' + rgb.slice(0, 3).map(channel => Math.round(channel).toString(16).padStart(2, '0')).join('');
+        latex = command === 'textcolor' ? `\\textcolor{${color}}{${latex}}` : `\\colorbox{${color}}{$${latex}$}`;
+    }
+    return latex;
+};
+
 const convertTreeToLatex = (root) => {
     var _a, _b;
     for (const normalizer of makeTreeNormalizers(root))
@@ -5124,7 +5173,11 @@ const convertTreeToLatex = (root) => {
                 continue;
             }
             stack.pop();
-            memo.set(frame.node, new MathMLElementToLatexConverterAdapter(frame.node).toLatexConverter().convert());
+            // Older KaTeX mclass output can wrap a row in a token element.
+            const rowInToken = ['mi', 'mo'].includes(frame.node.name) && frame.node.children.some(child => child.name === 'mrow');
+            const latex = rowInToken ? frame.node.children.map(child => memo.get(child) || '').join(' ')
+                : new MathMLElementToLatexConverterAdapter(frame.node).toLatexConverter().convert();
+            memo.set(frame.node, withMathColors(frame.node, latex));
         }
     }
     finally {
diff --git a/dist/index.mjs b/dist/index.mjs
index 31258499447701b2a195da3bcb430ba59a4059a0..f64c70474e43f544a1560257123b70c5a4c326c0 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1,4 +1,5 @@
 import { DOMParser } from '@xmldom/xmldom';
+import colorString from 'color-string';
 
 // During an iterative conversion (see tree-to-latex) every child is converted
 // before its parent and its LaTeX is stored in this memo. While a memo is
@@ -147,8 +148,8 @@ const allMathOperatorsByChar = {
     '&#x2037;': '```',
     '&#x2036;': '``',
     '&#x2035;': '`',
-    '&#x2034;': "'''",
-    '&#x2033;': "''",
+    '&#x2034;': '\\prime\\prime\\prime',
+    '&#x2033;': '\\prime\\prime',
     '&#x201F;': '``',
     '&#x201E;': ',,',
     '&#x201B;': '`',
@@ -196,12 +197,12 @@ const allMathOperatorsByChar = {
     '&#x266F;': '\\#',
     '&#x266E;': '',
     '&#x266D;': '',
-    '&#x2032;': "'",
+    '&#x2032;': '\\prime',
     '&lt;>': '<>',
     '**': '\\star\\star',
     '&#x2207;': '\\nabla',
     '&#x2202;': '\\partial',
-    '&#x2299;': '\\bigodot',
+    '&#x2299;': '\\odot',
     '&#xAC;': '\\neg',
     '&#x2222;': '\\measuredangle',
     '&#x2221;': '\\measuredangle',
@@ -603,8 +604,8 @@ const allMathOperatorsByChar = {
     '&#x2ACF;': '',
     '&#x2ACE;': '',
     '&#x2ACD;': '',
-    '&#x2ACC;': '\\underset{\\neq}{\\supset}',
-    '&#x2ACB;': '\\underset{\\neq}{\\subset}',
+    '&#x2ACC;': '\\supsetneqq',
+    '&#x2ACB;': '\\subsetneqq',
     '&#x2ACA;': '\\underset{\\approx}{\\supset}',
     '&#x2AC9;': '\\underset{\\approx}{\\subset}',
     '&#x2AC8;': '\\underset{\\sim}{\\supset}',
@@ -680,9 +681,9 @@ const allMathOperatorsByChar = {
     '&#x2A80;': '',
     '&#x2A7F;': '',
     '&#x2A7E;&#x338;': '\\bcancel{\\geq}',
-    '&#x2A7E;': '\\geq',
+    '&#x2A7E;': '\\geqslant',
     '&#x2A7D;&#x338;': '\\bcancel{\\leq}',
-    '&#x2A7D;': '\\leq',
+    '&#x2A7D;': '\\leqslant',
     '&#x2A7C;': '',
     '&#x2A7B;': '',
     '&#x2A7A;': '',
@@ -1037,7 +1038,7 @@ const allMathOperatorsByChar = {
     '&#x25B5;': '\\triangle',
     '&#x25B4;': '\\triangle',
     '&#x25B3;': '\\triangle',
-    '&#x25B1;': '\\square',
+    '&#x25B1;': '\\text{▱}',
     '&#x25B0;': '\\square',
     '&#x25AF;': '\\square',
     '&#x25AE;': '\\square',
@@ -1092,7 +1093,7 @@ const allMathOperatorsByChar = {
     '&#x2244;': '\\not\\simeq',
     '&#x2243;': '\\simeq',
     '&#x2241;': '\\not\\sim',
-    '&#x2226;': '\\not\\parallel',
+    '&#x2226;': '\\nparallel',
     '&#x2225;': '\\parallel',
     '&#x2224;': '\\not|',
     '&#x221D;': '\\propto',
@@ -1132,7 +1133,7 @@ const allMathOperatorsByChar = {
     '&#x220C;': '\\not\\in',
     '&#x2209;': '\\notin',
     '&#x2208;': '\\in',
-    '&#x2201;': 'C',
+    '&#x2201;': '\\complement',
     '&#x2204;': '\\nexists',
     '&#x2203;': '\\exists',
     '&#x2200;': '\\forall',
@@ -1274,8 +1275,8 @@ const allMathOperatorsByGlyph = {
     '‷': '```',
     '‶': '``',
     '‵': '`',
-    '‴': "'''",
-    '″': "''",
+    '‴': '\\prime\\prime\\prime',
+    '″': '\\prime\\prime',
     '‟': '``',
     '„': ',,',
     '‛': '`',
@@ -1322,12 +1323,12 @@ const allMathOperatorsByGlyph = {
     '♯': '\\#',
     '♮': '',
     '♭': '',
-    '′': "'",
+    '′': '\\prime',
     '<>': '<>',
     '**': '\\star\\star',
     '∇': '\\nabla',
     '∂': '\\partial',
-    '⊙': '\\bigodot',
+    '⊙': '\\odot',
     '¬': '\\neg',
     '∢': '\\measuredangle',
     '∡': '\\measuredangle',
@@ -1728,8 +1729,8 @@ const allMathOperatorsByGlyph = {
     '⫏': '',
     '⫎': '',
     '⫍': '',
-    '⫌': '\\underset{\\neq}{\\supset}',
-    '⫋': '\\underset{\\neq}{\\subset}',
+    '⫌': '\\supsetneqq',
+    '⫋': '\\subsetneqq',
     '⫊': '\\underset{\\approx}{\\supset}',
     '⫉': '\\underset{\\approx}{\\subset}',
     '⫈': '\\underset{\\sim}{\\supset}',
@@ -1805,9 +1806,9 @@ const allMathOperatorsByGlyph = {
     '⪀': '',
     '⩿': '',
     '⩾̸': '\\bcancel{\\geq}',
-    '⩾': '\\geq',
+    '⩾': '\\geqslant',
     '⩽̸': '\\bcancel{\\leq}',
-    '⩽': '\\leq',
+    '⩽': '\\leqslant',
     '⩼': '',
     '⩻': '',
     '⩺': '',
@@ -2165,7 +2166,7 @@ const allMathOperatorsByGlyph = {
     '▵': '\\triangle',
     '▴': '\\triangle',
     '△': '\\triangle',
-    '▱': '\\square',
+    '▱': '\\text{▱}',
     '▰': '\\blacksquare',
     '▯': '\\square',
     '▮': '\\blacksquare',
@@ -2220,7 +2221,7 @@ const allMathOperatorsByGlyph = {
     '≄': '\\not\\simeq',
     '≃': '\\simeq',
     '≁': '\\not\\sim',
-    '∦': '\\not\\parallel',
+    '∦': '\\nparallel',
     '∥': '\\parallel',
     '∤': '\\not|',
     '∝': '\\propto',
@@ -2260,7 +2261,7 @@ const allMathOperatorsByGlyph = {
     '∌': '\\not\\in',
     '∉': '\\notin',
     '∈': '\\in',
-    '∁': 'C',
+    '∁': '\\complement',
     '∄': '\\nexists',
     '∃': '\\exists',
     '∀': '\\forall',
@@ -2467,10 +2468,17 @@ const allMathOperatorsByGlyph = {
  * `<mi>`/`<mo>` converters to translate symbol content into LaTeX.
  */
 const allMathSymbolsByChar = {
+    '&#x22A5;': '\\perp',
+    '&#x2ACB;': '\\subsetneqq',
+    '&#x2ACC;': '\\supsetneqq',
+    '&#x2226;': '\\nparallel',
+    '&#x2201;': '\\complement',
+    '&#x2204;': '\\nexists',
+    '&#x25B1;': '\\text{▱}',
     '&#xA0;': '\\textrm{ }',
     '&#x2203;': '\\exists',
     '&#x2200;': '\\forall',
-    '&#x21D4;': '\\iff',
+    '&#x21D4;': '\\Leftrightarrow',
     '&#x21D2;': '=>',
     '&#xAC;': '\\neg',
     '&#x2124;': '\\mathbb{Z}',
@@ -2504,7 +2512,7 @@ const allMathSymbolsByChar = {
     '&#x2227;': '\\wedge',
     '&#x220F;': '\\prod',
     '&#x2211;': '\\sum',
-    '&#x2299;': '\\bigodot',
+    '&#x2299;': '\\odot',
     '&#x2297;': '\\bigoplus',
     '&#x2295;': 'o+',
     '&#x2218;': '@',
@@ -2564,6 +2572,12 @@ const allMathSymbolsByChar = {
  * converters to translate symbol content into LaTeX.
  */
 const allMathSymbolsByGlyph = {
+    '⊥': '\\perp',
+    '⫋': '\\subsetneqq',
+    '⫌': '\\supsetneqq',
+    '∦': '\\nparallel',
+    '∁': '\\complement',
+    '∄': '\\nexists',
     ' ': '\\textrm{ }',
     // Invisible operators (U+2061-U+2064): no visual rendering in LaTeX.
     '⁡': '',
@@ -2572,7 +2586,7 @@ const allMathSymbolsByGlyph = {
     '⁤': '',
     '∃': '\\exists',
     '∀': '\\forall',
-    '⇔': '\\iff',
+    '⇔': '\\Leftrightarrow',
     '⇒': '\\Rightarrow',
     '¬': '\\neg',
     '□': '\\square',
@@ -2601,7 +2615,7 @@ const allMathSymbolsByGlyph = {
     '∧': '\\wedge',
     '∏': '\\prod',
     '∑': '\\sum',
-    '⊙': '\\bigodot',
+    '⊙': '\\odot',
     '⊗': '\\bigoplus',
     '⊕': 'o+',
     '∘': '@',
@@ -2866,7 +2880,7 @@ const allMathSymbolsByGlyph = {
     '→': '\\rightarrow',
     '‡': '\\ddagger',
     '・': '\\cdot',
-    '▱': '\\square',
+    '▱': '\\text{▱}',
     '∆': '\\Delta',
     ἱ: 'i',
     '∡': '\\angle',
@@ -3584,6 +3598,9 @@ class MI {
         if (normalizedValue === ' ')
             return Character.apply(normalizedValue);
         const trimmedValue = normalizedValue.trim();
+        if (/^(sin|cos|tan|cot|sec|csc|arcsin|arccos|arctan|log|lg|ln|exp|lim|min|max|sup|inf|det|gcd)$/.test(trimmedValue)) {
+            return this.wrapInMathVariant('\\' + trimmedValue, this.getMathVariant(this._mathmlElement.attributes));
+        }
         const convertedChar = Character.apply(trimmedValue);
         const parsedChar = this.utf8Converter.convert(convertedChar);
         if (parsedChar !== convertedChar)
@@ -3604,7 +3621,7 @@ class MI {
             case 'italic':
                 return `\\mathit{${value}}`;
             case 'bold-italic':
-                return `\\mathbf{\\mathit{${value}}}`;
+                return `\\mathbfit{${value}}`;
             case 'double-struck':
                 return `\\mathbb{${value}}`;
             case 'bold-fraktur':
@@ -4425,6 +4442,8 @@ class MText {
      */
     convert() {
         const { attributes, value } = this._mathmlElement;
+        // Keep literal textbook glyphs and their combining marks in a single text run.
+        if (/^[\s⫋⫌⫽⃥▱∁]+$/u.test(value)) return new TextCommand(attributes.mathvariant).apply(value);
         return [...value]
             .map((char) => {
             // if is a letter, number or space, return it
@@ -4452,9 +4471,13 @@ class MText {
         }, [])
             .map((char) => {
             if (!char.isAlphanumeric) {
+                // DSH geometry glyphs remain text atoms when their weight changes.
+                if (attributes.mathvariant && ['⫽', '▱'].includes(char.value)) {
+                    return new TextCommand(attributes.mathvariant).apply(char.value);
+                }
                 return new MI({
                     name: 'mi',
-                    attributes: {},
+                    attributes,
                     children: [],
                     value: char.value,
                 }).convert();
@@ -4677,6 +4700,13 @@ class GenericUnderOver {
         if (childrenLength !== 2)
             throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
         const content = mathMLElementToLaTeXConverter(children[0]).convert();
+        if (name === 'mover' && this._mathmlElement.attributes.accent === 'true') {
+            const mark = children[1];
+            const stretch = mark.attributes.stretchy === 'true';
+            const command = { '⃗': 'vec', '→': 'overrightarrow', '^': stretch ? 'widehat' : 'hat',
+                'ˉ': 'bar', '‾': 'overline', '~': stretch ? 'widetilde' : 'tilde', '˙': 'dot', '¨': 'ddot' }[mark.value];
+            if (command) return `\\${command}{${content}}`;
+        }
         const accent = mathMLElementToLaTeXConverter(children[1]).convert();
         return this._applyCommand(content, accent);
     }
@@ -5104,6 +5134,25 @@ class TableAnnotator {
  * @param root - the root element of the tree to convert.
  * @returns the LaTeX string for the whole tree.
  */
+
+// DSH Teacher: retain colors and mathematical bold italics when reopening saved math.
+const withMathColors = (node, latex) => {
+    if (node.attributes?.fontweight === 'bold') latex = `\\boldsymbol{${latex}}`;
+    if (['mn', 'mo'].includes(node.name) || (node.name === 'mi' && node.children.length > 0)) {
+        const command = { bold: 'mathbf', italic: 'mathit', 'bold-italic': 'mathbfit' }[node.attributes?.mathvariant];
+        if (command) latex = `\\${command}{${latex}}`;
+    }
+    for (const [attribute, command] of [['mathcolor', 'textcolor'], ['mathbackground', 'colorbox']]) {
+        const value = node.attributes?.[attribute] || (attribute === 'mathcolor' ? node.attributes?.color : undefined);
+        if (!value) continue;
+        const rgb = colorString.get.rgb(value);
+        if (!rgb || rgb[3] !== 1) throw new Error('Equation colors must be opaque RGB colors');
+        const color = '#' + rgb.slice(0, 3).map(channel => Math.round(channel).toString(16).padStart(2, '0')).join('');
+        latex = command === 'textcolor' ? `\\textcolor{${color}}{${latex}}` : `\\colorbox{${color}}{$${latex}$}`;
+    }
+    return latex;
+};
+
 const convertTreeToLatex = (root) => {
     var _a, _b;
     for (const normalizer of makeTreeNormalizers(root))
@@ -5122,7 +5171,11 @@ const convertTreeToLatex = (root) => {
                 continue;
             }
             stack.pop();
-            memo.set(frame.node, new MathMLElementToLatexConverterAdapter(frame.node).toLatexConverter().convert());
+            // Older KaTeX mclass output can wrap a row in a token element.
+            const rowInToken = ['mi', 'mo'].includes(frame.node.name) && frame.node.children.some(child => child.name === 'mrow');
+            const latex = rowInToken ? frame.node.children.map(child => memo.get(child) || '').join(' ')
+                : new MathMLElementToLatexConverterAdapter(frame.node).toLatexConverter().convert();
+            memo.set(frame.node, withMathColors(frame.node, latex));
         }
     }
     finally {
